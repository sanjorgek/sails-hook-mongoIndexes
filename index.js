var mongo = require('mongodb').MongoClient;

module.exports = function indexes(sails) {
  return {
    //defaults: {},
    //configure: function () {},
    //initialize: function (cb) {},
    //routes: {}
  }
}